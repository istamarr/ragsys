use log::info;

pub fn dir_upload_report(){
    info!("Upload report ");
}

pub fn dir_upload_image(){
    info!("Upload report ");

    //option:
    // 1. upload dir/minio ---> ipfs
    // 2. ipfs opt not peer ---> showing ---> approve flag share to srv2
    

}